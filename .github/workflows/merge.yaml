# action.yml
name: 'Hello World'
on:
  workflow_dispatch:
  push:
    branches:
      - master
description: 'Greet someone and record the time'
jobs:
  build:
    runs-on: [self-hosted]
    steps:
      - name: start action
        run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - name: docker build
        run: docker build -t test:1.0 .
      - name: docker run
        run: docker run test:1.0
